/**
 * Конфігурація зарплатного калькулятора.
 * 
 * ⚠️ Для оновлення ставок, податків або надбавок —
 * змінюй значення ЛИШЕ в цьому файлі.
 * Логіка компонентів і розрахунків не має змінюватися.
 */

const salaryConfig = {
  // ============================
  // ЗАГАЛЬНІ НАЛАШТУВАННЯ
  // ============================
  tax: 0.23, // Податкова ставка (ЄСВ + ПДФО + ВЗ) - 23%

  // ============================
  // РОЛІ
  // ============================
  roles: {
    sv: {
      id: 'sv',
      name: 'SV',
      description: 'Супервайзер',
      available: true,
    },
    operator: {
      id: 'operator',
      name: 'Operator',
      description: 'Оператор',
      available: false,
    },
    '4lvl': {
      id: '4lvl',
      name: '4 LVL',
      description: '4-й рівень',
      available: false,
    },
  },

  // ============================
  // SV — конфігурація
  // ============================
  sv: {
    // Базовий оклад
    salary: 46560,

    // Нічна надбавка (%)
    nightBonus: 0.2,

    // Виплата за вау-кейс (після податків, грн)
    wowCasePayment: 400,

    // Максимальна кількість вау-кейсів
    maxWowCases: 5,

    // Стаж
    tenurePercentPerYear: 0.05, // +5% за кожен повний рік
    tenureBaseIncome: 63190,    // Цільовий дохід для розрахунку стажу (брутто, грн)

    // Кваліфікаційні рівні
    qualificationLevels: {
      1: { name: '1', bonus: 0 },
      2: { name: '2', bonus: 3620 },
      3: { name: '3', bonus: 7240 },
    },

    // Умови отримання надбавки за квал. рівень
    // minZone — мінімальна зона (включно), при якій надбавка діє
    qualificationRules: {
      1: { minZone: null }, // немає надбавки
      2: { minZone: 3 },   // зелена, салатова, жовта (зони 1-3)
      3: { minZone: 2 },   // зелена, салатова (зони 1-2); якщо жовта (3) — як за 2-й
    },

    // Зони рейтингу
    zones: [
      { id: 1, name: 'зелена', color: '#22c55e', textColor: '#ffffff', premium: 19160 },
      { id: 2, name: 'салатова', color: '#84cc16', textColor: '#1a1a1a', premium: 16730 },
      { id: 3, name: 'жовта', color: '#eab308', textColor: '#1a1a1a', premium: 14330 },
      { id: 4, name: 'рожева', color: '#f472b6', textColor: '#1a1a1a', premium: 11880 },
      { id: 5, name: 'червона', color: '#ef4444', textColor: '#ffffff', premium: 9440 },
    ],

    // Зона за замовчуванням
    defaultZone: 3,

    // Графіки
    schedules: ['2/2', '5/2'],
    defaultSchedule: '2/2',

    // Норма годин по місяцях
    // Ключі: рік -> графік -> масив з 12 значень (січень = індекс 0)
    normHours: {
      2026: {
        '2/2': [165, 154, 165, 165, 165, 165, 165, 165, 165, 165, 165, 165],
        '5/2': [176, 160, 176, 176, 168, 176, 184, 168, 176, 176, 168, 184],
      },
      // 2027: {
      //   '2/2': [null, null, ...],
      //   '5/2': [null, null, ...],
      // },
    },

    // Доступні роки
    availableYears: [2026],

    // Місяці з приміткою про неактуальні ставки
    disclaimerMonths: [0, 1], // січень, лютий (індекси)
    disclaimerText: 'Калькулятор рахує по актуальним ставкам, а в цей місяць вона була іншою.',

    // Валідація годин
    validation: {
      maxHours: 501,
      softMaxHours: 301,
      minHours: 0,
    },
  },
}

export default salaryConfig
